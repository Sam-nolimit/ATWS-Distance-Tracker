{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".env", // this must be specified if "paths" is specified.
    "paths": {
      "@env": ["node_modules/env"] // this mapping is relative to "baseUrl"
      
    }
  }
}
